<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/FullScreenContainer" >

    <ListView
        android:id="@+id/list_team_information_members"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <LinearLayout
        android:id="@+id/linear_team_information_guide"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" >

        <ImageView
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:layout_gravity="top"
            android:src="@drawable/background_upper_triangle" />

        <TextView
            style="@style/MessageBoxWhite"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="0dp"
            android:includeFontPadding="false"
            android:maxWidth="200dp"
            android:padding="6dp"
            android:text="@string/guide_invite_members" />
    </LinearLayout>

</FrameLayout>
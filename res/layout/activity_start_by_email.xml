<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/FullScreenContainer"
    android:orientation="vertical"
    tools:context=".TestActivity" >
    
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >
        
        <TextView
            android:id="@+id/button_start_by_email_login"
            style="@style/TabButtonSelected"
            android:text="@string/login"
            android:onClick="showLogin" />
        
        <TextView
            android:id="@+id/button_start_by_email_signup"
            style="@style/TabButtonNotSelected"
            android:text="@string/sign_up"
            android:onClick="showSignUp" />
    </LinearLayout>
    
    <FrameLayout
        android:id="@+id/frame_start_by_email_login"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />
    
    <FrameLayout
        android:id="@+id/frame_start_by_email_signup"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:visibility="gone" />

</LinearLayout>